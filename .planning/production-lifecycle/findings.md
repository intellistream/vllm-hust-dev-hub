# Findings
Initial clean branch at 0d62314. Remote main 6756c83 includes Responses/runtime and deterministic shutdown changes; using new branch from that exact main. No open PR; issues #49 (DSpark deployment) and #53 (hardware CI) do not duplicate this adapter. Existing docs place generic lifecycle under Dev Hub and product code under Sage/Workstation. This request explicitly authorizes generic production backend here; no product code changes.

Design: pre-existing exact Docker ID or devhub-managed-* notify user unit only, fixed private manager socket and immutable profile/config/artifact pins. Separate default-off production policy bound to state directory. One-use pipe-only lease, helper inherits flock; durable intent/settled/verified journal. Crucial: no success ACK (including nonzero CLI exit) is unknown, not evidence of failure; no automatic compensation from unknown intent. Surviving helper timeout uses EffectInProgress to avoid concurrent rollback. Completed health failures restore only exact owned resource and pinned baseline. Kernel cgroup and listener checks added for systemd. No actual daemon commands used by this task.
